import styled from 'styled-components';

export const __Footer = styled.footer`
  grid-area: footer;
  padding: 1.8em;
  text-align: center;
  position: relative;
  * + * {
    margin-top: .25em;
  }
`
